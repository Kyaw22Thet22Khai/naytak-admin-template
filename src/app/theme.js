/**
 * Theme configuration for the admin panel.
 * Consumed by the ThemeProvider in app/providers.js.
 */
export const THEME_CONFIG = {
  /** Initial color mode: "light" | "dark". */
  defaultMode: "light",
  /** Brand color used to derive component gradients. */
  primaryColor: "#2563eb",
};
